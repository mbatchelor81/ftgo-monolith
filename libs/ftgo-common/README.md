# ftgo-common

Shared, framework-light value objects used by every FTGO microservice:
`Money`, `PersonName`, `Address`, and their Jackson serialization support.

**Scaffold only.** The live implementation is in the repository-root
`ftgo-common/` module today. Migration to `libs/ftgo-common` is tracked by
EM-32.
