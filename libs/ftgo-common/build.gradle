// Placeholder build for the future shared `ftgo-common` library.
// The live implementation currently lives in `/ftgo-common` at the repo root
// and will be migrated here as part of EM-32.

apply plugin: 'io.spring.dependency-management'

description = 'FTGO shared value objects - scaffold; migration tracked in EM-32'
