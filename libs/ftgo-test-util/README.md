# ftgo-test-util

Shared test fixtures, assertion helpers, and in-memory infrastructure used
across service test suites.

**Scaffold only.** The live implementation is in the repository-root
`ftgo-test-util/` module today.
