# ftgo-common-jpa

Shared JPA configuration building blocks (converters, base entities, auditing
helpers) used by services that persist data via Spring Data JPA.

**Scaffold only.** The live implementation is in the repository-root
`ftgo-common-jpa/` module today. Migration to `libs/ftgo-common-jpa` is
tracked by EM-31.
