apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml
  - service.yaml
  - configmap.yaml
  - init-configmap.yaml
  - secret.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
