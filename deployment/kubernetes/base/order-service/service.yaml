apiVersion: v1
kind: Service
metadata:
  name: ftgo-order-service
  namespace: ftgo
  labels:
    app.kubernetes.io/name: ftgo-order-service
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ftgo-order-service
