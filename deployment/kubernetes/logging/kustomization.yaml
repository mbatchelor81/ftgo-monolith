apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# FTGO centralized logging (EFK) stack — see README.md.
namespace: logging
resources:
  - namespace.yaml
  - elasticsearch.yaml
  - kibana.yaml
  - fluentd-rbac.yaml
  - fluentd-configmap.yaml
  - fluentd-daemonset.yaml
  - ilm-policy.yaml
commonLabels:
  app.kubernetes.io/part-of: ftgo
  app.kubernetes.io/component: observability
