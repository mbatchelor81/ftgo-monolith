apiVersion: v1
kind: Service
metadata:
  name: example-service
  labels:
    app: example-service
    bounded-context: example
spec:
  type: ClusterIP
  selector:
    app: example-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
