# Local development profile overrides for Courier Service
# Activate with: --spring.profiles.active=local

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ftgo_courier_service
    username: root
    password: rootpassword
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    com.ftgo.courier: DEBUG
    org.hibernate.SQL: DEBUG
