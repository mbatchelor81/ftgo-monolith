apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
    bounded-context: order
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
