package net.chrisrichardson.ftgo.apigateway;

import org.junit.jupiter.api.Test;

/**
 * Smoke test that keeps the gateway skeleton exercising the standard test
 * task. Replace with real WebFlux integration tests once downstream stubs
 * are available (see EM-39 follow-up work).
 */
class ApiGatewayApplicationTest {

    @Test
    void contextBootstrap() {
        // Intentionally empty — verifies the test task compiles and runs.
    }
}
