# Local development profile overrides for Restaurant Service
# Activate with: --spring.profiles.active=local

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ftgo_restaurant_service
    username: root
    password: rootpassword
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    com.ftgo.restaurant: DEBUG
    org.hibernate.SQL: DEBUG
