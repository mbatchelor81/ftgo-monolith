apiVersion: v1
kind: Service
metadata:
  name: template-service
  labels:
    app: template-service
    part-of: ftgo
spec:
  type: ClusterIP
  selector:
    app: template-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
