apiVersion: v1
kind: Service
metadata:
  name: courier-service
  labels:
    app: courier-service
    bounded-context: courier
spec:
  type: ClusterIP
  selector:
    app: courier-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
