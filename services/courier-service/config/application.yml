spring:
  application:
    name: courier-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/ftgo_courier}
    username: ${DB_USER:ftgo}
    password: ${DB_PASSWORD:ftgo}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when-authorized
  metrics:
    tags:
      application: courier-service
      bounded-context: courier

logging:
  level:
    root: INFO
    com.ftgo.courier: INFO

# OpenAPI 3.0 metadata consumed by the shared `libs:ftgo-openapi` library.
# Keys are bound to `OpenApiProperties` (prefix `ftgo.openapi`).
ftgo:
  openapi:
    title: "FTGO Courier Service API"
    description: >
      Manages courier registration, availability updates, and delivery plan
      lookups. See the FTGO REST API standards for shared conventions.
    version: "1.0.0"
    contact:
      name: "FTGO Courier Team"
      email: "courier-team@ftgo.example.com"
      url: "https://github.com/mbatchelor81/ftgo-monolith/tree/master/services/courier-service"

# SpringDoc Swagger UI is served at /swagger-ui.html and the OpenAPI JSON
# at /v3/api-docs by default — no extra configuration needed here.
