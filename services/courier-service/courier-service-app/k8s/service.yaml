apiVersion: v1
kind: Service
metadata:
  name: courier-service
  labels:
    app: courier-service
    part-of: ftgo
spec:
  type: ClusterIP
  selector:
    app: courier-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
