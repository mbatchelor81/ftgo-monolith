package com.ftgo.restaurant;

/**
 * Placeholder entry point for the future standalone Restaurant microservice.
 *
 * Code will be migrated here from the legacy {@code ftgo-restaurant-service}
 * module as the microservices decomposition progresses. When that happens,
 * convert this class to a {@code @SpringBootApplication} and wire up the real
 * domain, web, and config packages under {@code com.ftgo.restaurant}.
 */
public final class RestaurantServiceApplication {

    private RestaurantServiceApplication() {
        // Scaffold class; no instantiation.
    }
}
