apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  labels:
    app: restaurant-service
    part-of: ftgo
spec:
  type: ClusterIP
  selector:
    app: restaurant-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
