# Local development profile overrides
# Activate with: --spring.profiles.active=local

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ftgo_CHANGEME_service
    username: root
    password: rootpassword
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    com.ftgo: DEBUG
    org.hibernate.SQL: DEBUG
