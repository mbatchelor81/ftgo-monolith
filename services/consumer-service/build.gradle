// Placeholder build for the future standalone Consumer microservice.
//
// The actual Consumer business logic currently lives in the legacy
// `ftgo-consumer-service` module at the repository root. As code is extracted
// during the microservices migration, move it under `src/main/java/com/ftgo/consumer`
// and wire up real dependencies here.
//
// See templates/service-template/build.gradle for the target shape.

apply plugin: 'io.spring.dependency-management'

description = 'FTGO Consumer microservice (scaffold - code extraction in progress)'

dependencies {
    // Intentionally minimal while the service is a scaffold. Add Spring Boot
    // starters and shared libs when the first bounded-context code lands here.
}
