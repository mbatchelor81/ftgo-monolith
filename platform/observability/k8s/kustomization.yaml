apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared observability collectors for FTGO (EM-42).
# Layered in per-overlay via ../../platform/observability/k8s. Not added to
# the platform `base/` kustomization so teams can opt out in prod if an
# external APM / managed Zipkin is already in place.
resources:
  - zipkin.yaml
