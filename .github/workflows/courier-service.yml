name: Courier Service CI

on:
  push:
    branches: [ main, feat/microservices-migration ]
    paths:
      - 'services/ftgo-courier-service/**'
      - 'services/ftgo-courier-service-api/**'
      - 'shared/**'
      - 'build-logic/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'
  pull_request:
    branches: [ main, feat/microservices-migration ]
    paths:
      - 'services/ftgo-courier-service/**'
      - 'services/ftgo-courier-service-api/**'
      - 'shared/**'
      - 'build-logic/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          validate-wrappers: false
          cache-read-only: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/feat/microservices-migration' }}

      - name: Build Courier Service API
        run: ./gradlew :services-ftgo-courier-service-api:build

      - name: Build Courier Service
        run: ./gradlew :services-ftgo-courier-service:build

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-courier-service
          path: |
            services/ftgo-courier-service/build/reports/tests/
            services/ftgo-courier-service-api/build/reports/tests/
          retention-days: 14
