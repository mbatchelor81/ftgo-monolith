name: "CI: Common Libraries"

on:
  push:
    branches: [main, master, feat/microservices-migration]
    paths:
      - "services/ftgo-common/**"
      - "services/ftgo-common-jpa/**"
      - "build-logic/**"
      - "gradle/libs.versions.toml"
      - "services/settings.gradle"
      - ".github/workflows/ci-common-libs.yml"
  pull_request:
    branches: [main, master, feat/microservices-migration]
    paths:
      - "services/ftgo-common/**"
      - "services/ftgo-common-jpa/**"
      - "build-logic/**"
      - "gradle/libs.versions.toml"
      - "services/settings.gradle"
      - ".github/workflows/ci-common-libs.yml"

permissions:
  contents: read

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            services/.gradle
          key: ${{ runner.os }}-gradle-common-${{ hashFiles('services/**/*.gradle', 'build-logic/**/*.gradle', 'gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-common-
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for Gradle wrapper
        run: chmod +x services/gradlew

      - name: Build ftgo-common
        working-directory: services
        run: ./gradlew :ftgo-common:build

      - name: Build ftgo-common-jpa
        working-directory: services
        run: ./gradlew :ftgo-common-jpa:build

      - name: Upload ftgo-common test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: common-lib-test-reports
          path: services/ftgo-common/build/reports/tests/
          retention-days: 14

      - name: Upload ftgo-common-jpa test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: common-jpa-lib-test-reports
          path: services/ftgo-common-jpa/build/reports/tests/
          retention-days: 14
